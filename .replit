modules = ["python-3.12"]
run = "uvicorn undefined:app --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-24_05"
packages = ["glibcLocales", "libxcrypt", "postgresql"]

[deployment]
run = ["sh", "-c", "uvicorn undefined:app --host 0.0.0.0 --port 3000"]

[workflows]
runButton = "Run Sports Betting API"

[[workflows.workflow]]
name = "Run Sports Betting API"
author = 40017536
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload"

[[ports]]
localPort = 8000
externalPort = 80
